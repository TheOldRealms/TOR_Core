# PR Notes

One file per completed epic, named `<module>-<epic>.md`, from
[`../templates/pr-notes.md`](../templates/pr-notes.md).

Each holds a copy-paste block for the PR body. **The human opens the PR** — these notes exist so
that takes two minutes and no re-reading of the diff.
